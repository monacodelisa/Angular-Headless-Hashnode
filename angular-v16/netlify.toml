[build]
  base = "/angular-v16"
  command = "npm run build:prod"
  publish = "dist/"
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF packages/angular-v16"

  [[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
