import { ClipboardCopyButtonDirective } from './clipboard-copy-button.directive';

describe('ClipboardCopyButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ClipboardCopyButtonDirective();
    expect(directive).toBeTruthy();
  });
});
